apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: pdc-kong-route
  namespace: i4trust-dev
spec:
  host: i4trust-dev-pdc-kong.apps.fiware-dev-aws.fiware.dev
  to:
    kind: Service
    name: i4trust-dev-pdc-kong-kong-proxy
  port:
    targetPort: 80
  tls:
    termination: edge

