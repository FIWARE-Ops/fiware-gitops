apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "gx-registry.fullname" . }}-mongo-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
