external-dns: 
  sources:
    - openshift-route
  domainFilters:
    - fiware.dev 
  policy: upsert-only
  extraArgs:
    - --aws-zone-type=public
  rbac:
    additionalPermissions:
      - apiGroups:
        - security.openshift.io
        resourceNames:
        - anyuid
        resources:
        - securitycontextconstraints
        verbs:
        - use