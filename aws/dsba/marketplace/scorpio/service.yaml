apiVersion: v1
kind: Service
metadata:
  name: marketplace-scorpio
  labels:
    app.kubernetes.io/name: marketplace-scorpio
    app.kubernetes.io/instance: marketplace-scorpio
spec:
  type: ClusterIP
  ports:
    # same as orion, makes other config easier
    - port: 1026
      targetPort: 9090
      protocol: TCP
      name: marketplace-scorpio
  selector:
    app.kubernetes.io/name: marketplace-scorpio
    app.kubernetes.io/instance: marketplace-scorpio
