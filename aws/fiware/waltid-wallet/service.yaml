apiVersion: v1
kind: Service
metadata:
  name: waltid-wallet-api
spec:
  type: ClusterIP
  ports: 
    - name: http
      protocol: TCP
      port: 3000
      targetPort: http-api
  selector:
    app.kubernetes.io/instance: waltid-wallet
    app.kubernetes.io/name: waltid-wallet