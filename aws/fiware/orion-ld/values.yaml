orion-ld:
  broker:
    db:
      hosts:
        - fiware-mongo-db-mongodb
      user: root
      existingSecret:
        name: mongodb-secret
        key: mongodb-password

  mongo:
    # we want to use the individually deployed mongodb
    enabled: false
