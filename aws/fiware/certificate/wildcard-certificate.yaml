apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fiware-wildcard-cert
spec:
  secretName: fiware-wildcard-tls-secret
  issuerRef: 
    kind: ClusterIssuer
    name: letsencrypt-aws-prod
  commonName: "*.fiware.dev"
  dnsNames:
    - "*.fiware.dev"