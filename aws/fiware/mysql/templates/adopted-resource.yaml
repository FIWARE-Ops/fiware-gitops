apiVersion: services.k8s.aws/v1alpha1
kind: AdoptedResource
metadata:
  name: {{ .Release.Name }}-adopted-mysql
spec:
  aws:
    nameOrID: {{ .Release.Name }}-mysql
  kubernetes:
    group: rds.services.k8s.aws/v1alpha1
    kind: DBInstance  