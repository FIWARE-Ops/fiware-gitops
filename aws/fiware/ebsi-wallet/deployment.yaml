kind: Deployment
apiVersion: apps/v1
metadata:
  name: ebsi-wallet
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: ebsi-wallet
      app.kubernetes.io/name: ebsi-wallet
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: ebsi-wallet
        app.kubernetes.io/name: ebsi-wallet
    spec:
      serviceAccountName: fiware-ebsi
      containers:
        - name: ebsi
          image: 'quay.io/wi_stefan/ebsi-wallet:latest'
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: static
              protocol: TCP
          volumeMount:
            - name: cache
              mountPath: /var/cache/nginx
      volumes:
        - name: cache
          emptyDir: {}
