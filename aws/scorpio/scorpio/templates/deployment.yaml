apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "scorpioBroker-aaio.fullname" . }}-2
  labels:
    {{- include "scorpioBroker-aaio.labels" . | nindent 4 }}
spec:
  {{- if not .Values.scorpio-broker-aaio.autoscaling.enabled }}
  replicas: {{ .Values.scorpio-broker-aaio.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "scorpioBroker-aaio.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.scorpio-broker-aaio.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "scorpioBroker-aaio.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.scorpio-broker-aaio.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "scorpioBroker-aaio.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.scorpio-broker-aaio.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.scorpio-broker-aaio.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.scorpio-broker-aaio.image.pullPolicy }}
          ports:
            - containerPort: 9090
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: {{ .Values.scorpio-broker-aaio.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.scorpio-broker-aaio.livenessProbe.periodSeconds }}
            failureThreshold: {{ .Values.scorpio-broker-aaio.livenessProbe.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: {{ .Values.scorpio-broker-aaio.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.scorpio-broker-aaio.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.scorpio-broker-aaio.readinessProbe.failureThreshold }}
    
          env:
            - name: MYSETTINGS_POSTGRES_DB
              value: {{ .Values.scorpio-broker-aaio.db.dbhost }}
            - name: MYSETTINGS_POSTGRES_PASSWORD
              value: {{ .Values.scorpio-broker-aaio.db.password }}
            - name: MYSETTINGS_POSTGRES_USER
              value: {{ .Values.scorpio-broker-aaio.db.user }}  
            - name: KAFKA_ADVERTISED_HOST_NAME
              value: kafka
            - name: KAFKA_ZOOKEEPER_CONNECT
              value: zookeeper:2181
            - name: KAFKA_ADVERTISED_PORT
              value: "9092"
            
          resources:
            {{- toYaml .Values.scorpio-broker-aaio.resources | nindent 12 }}
      {{- with .Values.scorpio-broker-aaio.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scorpio-broker-aaio.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.scorpio-broker-aaio.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
