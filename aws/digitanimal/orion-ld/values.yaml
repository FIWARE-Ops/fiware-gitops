orion-ld:
  broker:
    db:
      hosts:
        - mongo.cifkfl9vchfo.eu-central-1.docdb.amazonaws.com
      user: mongoadmin
      existingSecret:
        name: documentdb-secret
        key: mongodb-root-password
      noDevTrace: false

  mongo:
    # we want to use the individually deployed mongodb
    enabled: false
