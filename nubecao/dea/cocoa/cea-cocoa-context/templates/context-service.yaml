apiVersion: v1
kind: Service
metadata:
  name: context-json
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    app.kubernetes.io/name: context-json
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: context-json
